use anyhow::Result;

fn part_one(_input: &str) -> Result<usize> {
    Ok(0)
}

fn part_two(_input: &str) -> Result<usize> {
    Ok(0)
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn one() -> Result<()> {
        Ok(())
    }

    #[test]
    fn two() -> Result<()> {
        Ok(())
    }
}
